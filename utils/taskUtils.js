// const sampleTasks = require('../data/tasks').sampleTasks;

// exports.findTask = (taskId) => sampleTasks.find(task => task.id === taskId);
// exports.taskNotFound = (taskId) => res.status(404).send(`Task with id: ${taskId} not found.`);