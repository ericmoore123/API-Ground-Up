const multi = (v1, v2) => {
    return v1 * v2;
};

const divide = (v1, v2) => {
    return v1 / v2;
};

const add = (v1, v2) => {
    return v1 + v2;
};
const subtract = (v1, v2) => {
    return v1 - v2;
};

module.exports = {
    multi,
    divide,
    add,
    subtract
};